<section>
  <h4>Import Data</h4>
  {{#if this.importUnderstood}}
    <FileUpload
      accept=".json"
      @change={{this.importData}}
    />
  {{else}}
    <button {{action (toggle 'importUnderstood' this)}}>
      This will erase any local data
    </button>
  {{/if}}
</section>

<section>
  <h4>
    Export Data
  </h4>
  <button
    title="Download"
    class="primary"
    {{action this.exportData}}
  >
    {{svg-jar "download" class="icon"}}
  </button>
</section>

<section>
  <h4>
    Reset Data
  </h4>
  <button
    title="Delete all data"
    class="warning"
    {{action this.resetData}}
  >
    {{svg-jar "delete" class="icon"}}
  </button>
</section>