import reduce from 'track-your-goals/utils/reduce';
import { module, test } from 'qunit';

module('Unit | Utility | reduce', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = reduce();
    assert.ok(result);
  });
});
